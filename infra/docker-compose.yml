services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: starlabs
      POSTGRES_PASSWORD: starlabs
      POSTGRES_DB: starlabs
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: